const express = require('express');
const router = express.Router();

// Placeholder for recipient routes
router.get('/', async (req, res) => {
    res.json({ message: 'Recipient routes to be implemented' });
});

module.exports = router;
